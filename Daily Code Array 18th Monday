#include<iostream>
using namespace std;

int main() {
	//example 1: a 5-slot integer (number) array
	int scores[5] = { 78, 82, 93, 77, 90 };
	cout << "Here are your scores:" << endl;
	for (int i = 0; i < 5; i++)
		cout << scores[i] << "";
	cout << endl;

	//example 2: a 4-slot string (word) array
	string names[4] = { "Venessa", "Landyn", "Eli", "Carlos" };
	//add a for loop to print names here!

	for (int i = 0; i < 4; i++)
		cout << names[i] << " ";
	cout << endl;

	////example 3: a boolean(true/false) array initialized with user input
	//bool wins[8];
	//cout << "Enter 1 for win and 0 for loss:" << endl;
	//for (int i = 0; i < 8; i++)
	//	cin >> wins[i];
	//cout << "Your wins/losses:" << endl;
	////add a for loop here print the values!

	//for (int i = 0; i < 8; i++)
	//	cout << wins[i] << " ";
	//cout << endl;

	int wins[10];
	cout << "Enter 10 numbers:" << endl;
	for (int i = 0; i < 10; i++)
		cin >> wins[i];
	cout << "The average is "<< (wins[0] + wins[1] + wins[2] + wins[3] + wins[4] + wins[5] + wins[6] + wins[7] + wins[8] + wins[9]) / 10 << endl;
	//add a for loop here print the values!


	int temp = wins[0];

	for (int i = 0; i < 9; i++) {
		if (wins[i+1] > wins[i])
			temp = wins[i+1];
	}
	
	cout << temp << endl;
	
	
	
	//check if wins[1] is bigger than wins[0]
	// 
	//if yes, put wins[1] into temp

	


}
